# Copy to .env.local and fill in your values

# URL of your FastAPI backend (Railway, Render, etc.)
# Leave empty when using next.config.mjs rewrites (recommended for Vercel)
NEXT_PUBLIC_API_URL=http://localhost:8000
